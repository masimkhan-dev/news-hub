// src/main.jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './app.jsx';  // Ensure the path and casing match

ReactDOM.createRoot(document.getElementById('app')).render(<App />);
